{
  "name": "peckish",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp -r data dist/data",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "scrape": "pushd scraper && npm run build && npm run scrape",
    "translate": "pushd scraper && npm run build && npm run translate",
    "test": "vitest run",
    "verify": "pushd scraper && npm run clean && npm run build && npm run scrape && npm run translate && popd && pushd voting && npm run clean && npm run build && npm run generate && npm run health-check && npx vitest run && popd && vitest run"
  },
  "devDependencies": {
    "@types/leaflet": "1.9.21",
    "jsdom": "29.0.1",
    "typescript": "5.9.3",
    "vite": "8.0.5",
    "vitest": "4.1.0"
  },
  "dependencies": {
    "leaflet": "1.9.4",
    "lucide": "1.7.0",
    "nostr-tools": "2.23.3"
  }
}
